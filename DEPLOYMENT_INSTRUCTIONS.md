# تعليمات نشر التحديثات على Hostinger

بما أنك قمت برفع التعديلات إلى GitHub، الخطوة التالية هي سحب هذه التعديلات إلى السيرفر (Hostinger VPS) وتشغيلها.

## الخطوات:

1. **الدخول إلى التيرمينال (Terminal)**:
   - قم بالدخول إلى لوحة تحكم Hostinger.
   - اذهب إلى **VPS** > اختر السيرفر الخاص بك.
   - اختر **Terminal** (يمكنك استخدام المتصفح أو برنامج مثل PuTTY/Terminal).

2. **الانتقال لمجلد المشروع**:
   نفذ الأمر التالي للانتقال لمجلد المشروع (تأكد من المسار الصحيح، غالباً يكون في `/var/www` أو `/home`):
   ```bash
   cd /path/to/your/project
   # مثال (تأكد من المسار الفعلي):
   # cd /var/www/silver-clean
   ```

3. **سحب التحديثات من GitHub**:
   ```bash
   git pull origin main
   ```
   *(إذا واجهت أي مشاكل في الصلاحيات أو التعديلات المحلية، راجع قسم الملاحظات أدناه)*

4. **تحديث قاعدة البيانات (Migration)**:
   بما أننا أضفنا أعمدة جديدة (`booking_days_limit`)، يجب تشغيل أمر الترحيل لتطبيق التغييرات على قاعدة البيانات في السيرفر:
   ```bash
   flask db upgrade
   ```
   *(أو `python3 -m flask db upgrade` حسب إعداداتك)*

5. **إعادة تشغيل التطبيق**:
   لتطبيق التغييرات في الكود، يجب إعادة تشغيل Gunicorn أو الخدمة:
   ```bash
   sudo systemctl restart silver
   # استبدل 'silver' باسم الخدمة الفعلي إذا كان مختلفاً (مثلاً 'gunicorn' أو 'app')
   ```

## ملاحظات هامة:
- **إذا واجهت مشكلة في `git pull` بسبب تعديلات محلية في السيرفر**:
  يمكنك إجبار التحديث من GitHub وحذف أي تعديلات يدوية قمت بها مباشرة على السيرفر باستخدام:
  ```bash
  git reset --hard origin/main
  git pull
  ```
  *(تحذير: هذا سيحذف أي تعديلات يدوية قمت بها مباشرة على السيرفر ولم ترفعها لـ GitHub)*

- **إذا ظهر خطأ TemplateSyntaxError**:
  تأكد من إعادة تشغيل الخدمة (`systemctl restart`) لأن Gunicorn قد يحتفظ بنسخة قديمة من ملفات القوالب في الذاكرة.
